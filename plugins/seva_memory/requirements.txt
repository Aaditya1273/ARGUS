# Plugin dependencies (installed into LangBot's plugin runtime environment)
openai>=1.40.0
qdrant-client>=1.9.0
slack-sdk>=3.26.0

# Webhook server dependencies (separate process — pip install separately)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.5.0
