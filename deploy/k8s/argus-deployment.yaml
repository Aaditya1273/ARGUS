apiVersion: apps/v1
kind: Deployment
metadata:
  name: argus-control-plane
  labels:
    app: argus-control-plane
spec:
  replicas: 3
  selector:
    matchLabels:
      app: argus-control-plane
  template:
    metadata:
      labels:
        app: argus-control-plane
    spec:
      containers:
      - name: argus-control-plane
        image: argus/control-plane:v1.0.0
        ports:
        - containerPort: 8080
        - containerPort: 4317
        env:
        - name: ARGUS_REDIS_URL
          value: "redis://argus-redis:6379"
        livenessProbe:
          httpGet:
            path: /api/v1/health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /api/v1/health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
